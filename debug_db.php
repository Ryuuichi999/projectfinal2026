<?php
require 'includes/db.php';

$result = $conn->query("DESCRIBE sign_requests");
if ($result) {
    while ($row = $result->fetch_assoc()) {
        echo $row['Field'] . " - " . $row['Type'] . "\n";
    }
} else {
    echo "Error: " . $conn->error;
}
